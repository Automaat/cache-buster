version: "2"

run:
  timeout: 5m
  go: "1.26.1"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gosec
    - unconvert
    - gocritic
    - prealloc
  settings:
    govet:
      enable-all: true
    revive:
      rules:
        - name: exported
          disabled: false
    gosec:
      excludes:
        - G204
        - G302
        - G702

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true

issues:
  uniq-by-line: true
